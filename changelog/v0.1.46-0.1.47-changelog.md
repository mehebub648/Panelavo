# 0.1.46 to 0.1.47

- Fixed rootful migration identity detection for `init: true` and privilege-dropping entrypoints.
- A unique non-root application child is now used instead of its root init wrapper; multiple non-root identities still block unsafe migration.
