# 0.1.42 to 0.1.43

- Fixed trusted `setup.sh` upgrades leaving an older failed updater-state record after successfully deploying a newer release.
- Setup now atomically records the installed version and commit as current while preserving the configured update repository, branch, and log path.
