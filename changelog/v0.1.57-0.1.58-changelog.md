# v0.1.57 to v0.1.58

- Reuse an existing active CloudPanel Admin when the configured Super Admin email already belongs to that account under a different username.
- Reject username or email collisions with non-Admin or inactive CloudPanel accounts instead of failing later with a generic duplicate-value error.

No migration is required.
