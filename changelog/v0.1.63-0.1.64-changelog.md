# v0.1.63 → v0.1.64

- Replaced the nested site-section timeout ladder with one explicit lookup table.
- Preserved the existing bounds for Operations, Backups, File Manager, Git, Terminal, Environment, and the short default used by other sections.
- Added a regression test for every timeout entry.

No runtime behavior or configuration changed.
