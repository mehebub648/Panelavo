# 0.1.39

- Configured the panel Nginx proxy to allow synchronous Operations and Backups requests to run for their full bounded duration.
- Re-running `setup.sh` applies the proxy timeout to an existing installation and validates the Nginx configuration before reloading it.
