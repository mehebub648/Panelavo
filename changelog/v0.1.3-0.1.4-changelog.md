# 0.1.3 to 0.1.4

- Activate the fail2ban maintenance guard automatically without relying on `SSH_CONNECTION` surviving sudo.
- Recover live SSH connection metadata from the installer process ancestry.
- Fall back to the configured sshd port when session metadata is unavailable.
- Clarify that provider-console commands are recovery-only, not customer installation steps.

Customers can use the documented `git clone` and `sudo bash setup.sh` flow without a separate security preflight.
