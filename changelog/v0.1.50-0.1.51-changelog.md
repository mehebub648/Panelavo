# 0.1.50 to 0.1.51

- Fixed rootless cutover treating `health: starting` as ready.
- Cutover now waits for every service to become running and healthy before probing HTTP.
- Rootless and rollback HTTP verification now retries startup races instead of failing on the first connection reset or empty response.
