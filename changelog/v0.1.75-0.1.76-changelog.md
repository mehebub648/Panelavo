# v0.1.75 → v0.1.76

- Added per-site, ordered after-pull deployment plans in the Git section.
- Limited plans to ten existing safe Operations with validated script/process identifiers and no free-form shell.
- Re-resolved every operation against current detection and preflight state before execution.
- Serialized the Git pull and all post-pull steps under the existing per-site operations lock.
- Stopped plans on the first failed operation and surfaced the deployment outcome with the Git result.
- Documented release-directory zero-downtime deployment and staging/clone environments as explicit roadmap items.

This release raises the broker protocol from 8 to 9. Existing installations must run `sudo bash setup.sh` from this trusted checkout before the in-panel updater can install it.
