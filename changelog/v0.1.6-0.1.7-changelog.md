# 0.1.6 to 0.1.7

- Stopped remote setup from automatically activating an inactive UFW firewall.
- Setup still prepares SSH, web, application, and CloudPanel firewall rules.
- Added `ENABLE_UFW=true` as an explicit activation opt-in.
- Documented provider-console recovery for installations locked out by an older setup version.

Existing active firewalls continue to be updated normally.
