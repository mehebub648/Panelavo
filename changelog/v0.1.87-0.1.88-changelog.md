# v0.1.87 → v0.1.88

## Fixed

- Replaced the installer's `ls | grep` PHP-runtime discovery pipelines with bounded directory searches so the CI ShellCheck gate passes without changing version selection.

## Compatibility

- PHP runtime discovery still prefers the newest installed version no later than PHP 8.4 and falls back to the newest installed version.
- No configuration or data migration is required.
