# v0.1.117 → v0.1.118

- Replace the sidebar server dropdown with a dedicated, searchable server-card selection page. Keep nicknames, current-server context, and local access available during remote outages.
- Fix Node.js DNS lookup callback compatibility causing `Invalid IP address: undefined` when connecting servers. Pin validated addresses with the correct lookup result shape and disable pooled outbound connections.
- Add a Super Admin-only Panel address setting. Verify that the proposed public HTTPS origin reaches the same panel before changing it; DNS, the CloudPanel domain alias, and its certificate must already be configured.
- Preserve stable server IDs and keys across address changes using signed peer updates and new-origin identity probes. Journal partial progress for retry; keep the old address active until all peers acknowledge. Both old and new domains must remain available during migration.
- Keep previous panel domains protected from site management. Canonical MCP URLs follow the verified address; personal OAuth clients may require reconnection. Address migration requires all connected panels on v0.1.118 or newer; broker protocol 24 and installer are unchanged.
