# 0.1.43 to 0.1.44

- Fixed rootless readiness and cleanup failing because PHP does not provide an `is_socket()` filesystem helper.
- Unix socket detection now uses a bounded filesystem-type check and is covered by the broker rootless self-test on Linux.
