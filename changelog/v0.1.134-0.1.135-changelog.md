# v0.1.135

- Prevent Windows shell line endings from breaking update startup.
- Record worker launch and early-exit failures; recover abandoned queued updates after two minutes, including Fleet-triggered updates.
- Preserve running builds and the existing PM2 reload handoff.
