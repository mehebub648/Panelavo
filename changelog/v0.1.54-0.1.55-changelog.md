# 0.1.54 to 0.1.55

- Added separate Root directory and Serving directory settings.
- Git, File Manager, Terminal defaults, Environment, Operations, cron path guidance, and backups now use the Root directory.
- CloudPanel and NGINX continue to use the Serving directory.
- Existing sites keep their current directory behavior until a separate root is saved.
- Upgraded the root-broker protocol to 5. Run `sudo bash setup.sh` from the trusted checkout before using this release.
