# v0.1.70 → v0.1.71

- Added Super Admin-managed SMTP email and generic HTTPS webhook notification channels.
- Added live test delivery, Slack/Discord-compatible webhook fields, and bounded channel timeouts.
- Stored SMTP and webhook configuration in an AES-256-GCM encrypted private store and kept saved passwords out of browser responses.
- Added Nodemailer for standards-compliant SMTP delivery.

No migration is required. Existing `CREDENTIALS_ENCRYPTION_KEY` or `SESSION_SECRET` configuration protects the new store.
