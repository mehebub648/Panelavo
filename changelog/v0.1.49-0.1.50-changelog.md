# 0.1.49 to 0.1.50

- Added reversible ACL access for mapped non-root container identities during rootless bind migration.
- Mapped users receive traverse-only ancestor access and inherited `rwX` within their writable binds; conflicting pre-existing entries block migration.
- Rollback removes migration-added mapped-user ACLs before restoring the original ownership and site-user ACL invariant.
