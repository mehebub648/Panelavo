# v0.1.74 → v0.1.75

- Added user-managed, scoped, expiring API tokens with one-time secret display.
- Stored token HMAC digests instead of reusable plaintext credentials.
- Added `sites:read` and `sites:write` scopes, revocation, expiry, and per-token throttling.
- Added versioned site listing and site-section read/mutation endpoints under `/api/v1`.
- Revalidated the current CloudPanel account, role, and site assignments on every API request.

The API does not expose the browser Terminal. Existing site-operation validation and broker authorization remain authoritative.
