# 0.1.31

- Remove empty `ipam: {}` placeholders after Compose JSON is decoded and re-encoded for an ephemeral runtime configuration.
- Keep non-empty custom network IPAM settings unchanged.
- Add regression coverage for both null and empty generated IPAM shapes.
