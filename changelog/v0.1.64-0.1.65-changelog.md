# v0.1.64 → v0.1.65

- Added one request-aware public-IP helper that consistently parses the first forwarded host, strips a port, and falls back to the request URL hostname.
- Replaced the repeated preamble in site creation, linked-service creation, deletion cleanup, domain management, and DNS routes.
- Added forwarded-host and fallback parsing coverage.

No configuration change is required.
