# v0.1.67 → v0.1.68

- Added a Super Admin-only Audit page with user, site/target, action, outcome, and date-range filters plus pagination.
- Added `GET /api/audit` over the existing bounded `readAuditEvents()` path.
- Surface retained-ledger verification as an `Integrity: verified` badge and show verification issues when hashes, links, or the persisted head fail.
- Added query-boundary and API authorization coverage.

No migration or configuration change is required; existing `.data/audit` events appear automatically.
