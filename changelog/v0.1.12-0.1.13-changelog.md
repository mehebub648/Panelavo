# 0.1.13

- Fixed SSH Git operations so the site user's home, deployment key, and `known_hosts` file remain selected after `sudo` resets the environment.
- New Git host keys are now accepted and persisted explicitly for the site user, while existing host keys continue to be verified.

After updating Panelavo, no repository or deployment-key changes are required. Retry the Git operation.
