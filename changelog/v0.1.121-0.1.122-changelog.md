# v0.1.122

- Load only the requested remote section, avoiding full inventories on Settings, Resources, Information and Updates.
- Deduplicate account/site reads within each server render, coalesce IP/readiness checks, briefly cache the expensive host software inventory behind live authorization, and show page loading inside the panel shell.
- Add remote panel address changes, server sharing, and user invitations through the existing signed, scoped Super Admin connection. Preserve DNS/TLS identity proofs, acknowledgements, live account validation and replay protection.
- Reuse local connection and invitation services; keep legacy links from silently gaining panel administration permissions.

- Fix the 15-second blocking navigation overlay on remote tabs by completing navigation on query-string changes as well as pathname changes. Ignore same-page anchor history navigation.
