# v0.1.56 to v0.1.57

- Make trusted setup repair application-tree ownership before deployment.
- Keep files deployed by root-run setup owned by the panel site user.
- Check updater directory writability before spending time on a staged build.
- Stop updater synchronization from preserving staged owner or group metadata.

Existing installations with ownership drift should rerun `sudo bash setup.sh` once from a trusted checkout.
