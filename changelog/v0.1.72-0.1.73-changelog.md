# v0.1.72 → v0.1.73

- Added self-service TOTP enrollment and disable controls to Profile.
- Kept pending enrollment secrets encrypted at rest with a ten-minute expiry.
- Required the current password to begin enrollment and a valid authenticator code before CloudPanel is changed.
- Added a narrowly allow-listed broker action for CloudPanel MFA changes.

This release raises the broker protocol from 7 to 8. Existing installations must run `sudo bash setup.sh` from this trusted checkout before the in-panel updater can install it.
