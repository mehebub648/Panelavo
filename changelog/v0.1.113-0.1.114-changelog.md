# v0.1.113 → v0.1.114

## WireGuard startup compatibility

- Load the separate root-only gateway private key from `PostUp`, after distro `wg-quick` has created `pnlwg0`. Ubuntu's distribution-maintained WireGuard tools execute `PreUp` before interface creation, which caused the first live installation attempt to fail safely and roll back.
- Extended the broker VPN self-test to reject the incompatible pre-interface key-loading hook.
