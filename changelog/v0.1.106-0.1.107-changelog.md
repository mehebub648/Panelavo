# Panelavo v0.1.107

## Fixed

- MCP background jobs now report `failed` when a resolved server execution has a nonzero aggregate or step exit code, or a reported timeout.
- Failed execution results remain available in the bounded job summary for diagnosis instead of being mislabeled as successful.

## Compatibility

- No configuration, broker protocol, or migration changes are required.
