# 0.1.22 to 0.1.23

- Allow the standalone `compose-deploy` Operations command through the shared API request schema.
- Fix the **Build & start services** button returning `Invalid input` before Docker execution.
- Add regression coverage for the exact build-operation request payload.
