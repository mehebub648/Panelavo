# 0.1.30

- Remove Compose-generated `ipam: null` placeholders from ephemeral runtime configurations.
- Preserve explicit network IPAM mappings and the original Compose file.
- Add regression coverage for default and configured networks.
