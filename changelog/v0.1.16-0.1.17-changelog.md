# 0.1.17

- Fixed updates remaining permanently locked because PM2 terminated the update worker before it could record completion.
- Added a PID-bound reload handoff that the replacement Panelavo process completes automatically.
- Added recovery for legacy stuck updates when the installed and remote commits already match, including stale lock cleanup.
- Kept panel pages renderable behind the maintenance overlay while authenticated API operations remain blocked.

Existing installations stuck after upgrading to 0.1.16 recover automatically after 0.1.17 is deployed and receives a panel request.
