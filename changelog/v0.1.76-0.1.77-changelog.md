# v0.1.76 → v0.1.77

- Re-resolved after-pull Operations from the newly pulled application tree instead of the pre-pull tree.
- Kept the pull, refreshed preflight, and operation execution inside the same per-site lock.

This release raises the broker protocol from 9 to 10. Existing installations must run `sudo bash setup.sh` from this trusted checkout before the in-panel updater can install it.
