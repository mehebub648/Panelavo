# v0.1.120 — Connected panel handoff fix

- Fix the full-panel switch being refused by Chrome: remove the child iframe sandbox that changed the browser handoff context before the Node could validate the manager origin.
- Keep the exact pinned Node in the wrapper's `frame-src` and `form-action`, require a short-lived signed one-use ticket, isolate the delegated cookie, and keep Node pages frameable only by their authenticated manager.
- Keep peer protocol 1, broker protocol 24, and `setup.sh` unchanged.
