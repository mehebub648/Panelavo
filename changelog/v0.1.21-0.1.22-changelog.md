# 0.1.21 to 0.1.22

- Add a dedicated Docker Compose **Build & start services** operation.
- Run `docker compose up -d --build --remove-orphans` for that operation and verify the configured website entry port afterward.
- Keep the existing plain start operation for cases that do not require rebuilding images.
