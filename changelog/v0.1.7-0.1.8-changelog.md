# 0.1.7 to 0.1.8

- Setup now leaves the active fail2ban `sshd` jail running by default.
- The optional client-IP exemption remains temporary and is removed on exit.
- Added recovery instructions for older runs that left fail2ban with no jails.

No configuration migration is required.
