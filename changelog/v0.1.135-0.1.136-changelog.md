# v0.1.136

- Fix Domains tab certificate issuance silently adding unselected apex/www companion names. Manual issuance now validates and requests only the selected site domains plus the required system domain, matching the explicit alternative-name certificate form.
- Add regression coverage for www and apex selections, duplicate names, and unrelated domains.
- Add a per-pair toggle to redirect a bare alias to www or serve both addresses without a redirect. Preserve paths, queries, and ACME access; remove stale rules when either alias is removed.
- Requires broker protocol 25, installed through trusted setup before application update.
