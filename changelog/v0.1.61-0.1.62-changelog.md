# v0.1.61 → v0.1.62

- Added one private JSON-store implementation with serialized atomic writes and predictable fallback behavior.
- Migrated site metadata, application-root, site-type, panel-role, and panel-settings stores without changing their existing filenames or schemas.
- Added round-trip, invalid-data fallback, and overlapping-write coverage.

No data migration or configuration change is required.
