# 0.1.16

- Made the selected Docker site type authoritative in Operations instead of exposing host npm and PM2 actions from files inside the Docker project.
- Added a Docker Compose one-click deployment using image pull followed by `docker compose up --build`.
- Applied the Docker action policy on the server so non-Compose commands cannot be invoked manually.

Non-Docker sites continue to detect actions from their project files.
