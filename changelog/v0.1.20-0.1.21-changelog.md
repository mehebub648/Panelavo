# 0.1.20 to 0.1.21

- Detect the CloudPanel-configured website entry port and compare it with live site-owned listeners.
- Select an unambiguous Docker Compose entry service and map its container port privately to the website port at runtime without editing the source Compose file.
- Block ambiguous Compose entry ports with explicit label guidance and report additional service ports for connected reverse-proxy sites.
- Pass the configured port to managed Node.js and Python processes and verify port-based deployments before reporting success.
