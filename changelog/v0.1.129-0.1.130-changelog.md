# v0.1.130

- Publish the pending connected-server workflow: independent incoming and outgoing connections, server nicknames, token-based sharing, and native server switching with legacy route redirects.
- Add verified panel-address changes with signed peer acknowledgements and resumable migration state.
- Enable scoped remote panel settings, server sharing, and user invitations while preserving live Super Admin authorization and existing site boundaries.
- Include navigation loading fixes, connection and address regression tests, and the previously untracked release history.
- Broker protocol remains 24. Preserve all of `.data` and `.env.local`; deployment requires only the Panelavo process reload.
