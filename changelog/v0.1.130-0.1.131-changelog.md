# v0.1.131

- Check the selected PHP version's actual next CloudPanel pool port instead of blocking every PHP website when legacy applications occupy the PHP port range.
- Serialize Panelavo website creation and keep occupied or reserved ports blocked before any site is created.
- Explain PHP port conflicts with an actionable version-selection message. Admins retain full control over assigned and self-created sites; no global access is granted.
- Install the updated root-owned bridge when deploying this fix. Broker protocol remains 24; no hosted application or database restart is required.
