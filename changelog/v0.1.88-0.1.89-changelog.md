# v0.1.88 → v0.1.89

## Added

- Added expiring, named MCP bearer tokens that users can generate from **AI access**, copy once, review, and revoke immediately.
- Added copy-ready Codex setup for Windows and macOS/Linux using `bearer_token_env_var`, plus connection details for other Streamable HTTP MCP clients.
- Kept browser-based OAuth as an optional advanced setup path.
- Bound personal tokens to the canonical panel MCP resource and the user's stable CloudPanel identity, with live role and website-access revalidation on every request.
- Store only keyed token digests and non-secret metadata in `.data/mcp-oauth.json`.

## Fixed

- Narrowed CloudPanel's direct-file `/.well-known` Nginx location to ACME challenges so MCP OAuth discovery reaches Panelavo.

## Compatibility

- Existing OAuth MCP connections continue to work unchanged.
- No new server environment variable, listener, firewall rule, dependency, or data migration is required.
