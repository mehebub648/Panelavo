# v0.1.77 → v0.1.78

- Preserved printable Unicode support in configured password policies while continuing to reject control characters.
- Fell back to the safe 60-minute session lifetime when a legacy `SESSION_MAX_AGE_SECONDS` value is malformed.

No migration is required.
