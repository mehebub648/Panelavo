# v0.1.71 → v0.1.72

- Added debounced CPU, memory, and disk threshold alerts with matching recovery notices.
- Added per-site HTTPS uptime checks, persisted last-check state on the sites list, and configurable consecutive-failure handling.
- Added six-hour TLS expiry checks and panel update-availability notifications.
- Added immediate scheduled-backup failure notifications.
- Added Super Admin monitoring controls and per-site uptime controls.

Monitoring runs in the existing single PM2 process and persists state in `.data/monitoring.json`. No migration or new inbound port is required.
