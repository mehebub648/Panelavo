# 0.1.136 → 0.1.137

- Simplify Git & Deploy and Operations around application status, saved deployment results and expandable advanced tools; load logs/schedules on demand.
- Add shared background deployment jobs and tested-commit CI integration with website-specific expiring tokens and signed Fleet support.
- Require clean fast-forward Git updates, preserve recovery stashes and serialize managed Git operations with deployment.
- Reject HTTP errors, wait for healthy Compose services, validate ordered custom steps and preserve failed output.
- Correct environment drift guidance and effective panel proxy timeouts. Preserve legacy recipes.
- Restart existing Node processes without creating duplicates, and keep token revocation effective during concurrent requests.
- Require broker protocol 26; preserve runtime data during the separately targeted production rollout.
