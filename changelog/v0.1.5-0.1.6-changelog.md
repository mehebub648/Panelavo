# 0.1.5 to 0.1.6

- Added a fail2ban configuration-reload fallback when restarting the paused `sshd` jail.
- Updated recovery guidance to use the same reliable restore command.

No configuration migration is required.
