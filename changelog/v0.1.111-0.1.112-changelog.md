# v0.1.111 → v0.1.112

## SSL issuance error messaging fix

- Fixed privileged error mapping so certificate issuance and "Recheck DNS & secure" failures no longer get rewritten to the database-name validation message.
- Kept the existing 2–50 character guidance for real database name/username validation errors only.
