# 0.1.40

- Site section failures (backups, Operations, Git, and other `manage-section` actions) now surface the CloudPanel bridge's specific reason instead of collapsing every failure to a generic "CloudPanel could not apply the change." 502. A failed backup now reports the actual cause (e.g. a failed file archive or database export) so operators can act on it.
