# v0.1.119 — Full connected-panel access

- New, explicitly authorized connections open the Node's actual Super Admin interface with matching navigation, data, settings, user management, and website tools, while retaining the managing panel's top-level browser domain.
- Add a signed finite session-handoff action, encrypted hashed one-use 30-second tickets, exact-origin framing, and isolated Secure/HttpOnly partitioned sessions. Revalidate connection ownership and live Super Admin status on each authenticated request; preserve native password checks and confirmations.
- Keep existing limited grants unchanged until a fresh token is submitted. Preserve server nicknames, many-to-many direct connections, address migration, local sessions, and hosted applications.
- Keep peer protocol 1, broker protocol 24, and setup.sh unchanged.
