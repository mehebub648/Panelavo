// Compatibility re-export for existing UI imports. The policy lives in lib so
// server routes and MCP enforce the same linked-service boundary as the UI.
export { SERVICE_SECTIONS } from "@/lib/site-sections";
